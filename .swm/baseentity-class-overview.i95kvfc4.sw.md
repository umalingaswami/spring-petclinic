---
title: BaseEntity Class Overview
---
# Introduction

This document explains the design and purpose of the <SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="33:4:4" line-data="public class BaseEntity implements Serializable {">`BaseEntity`</SwmToken> class in the Spring Petclinic model layer. It answers these questions:

1. Why use a base class for entities?
2. How is the entity identifier managed?
3. What utility methods does the base class provide?

# why a base class for entities?

<SwmSnippet path="/src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" line="18">

---

<SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="33:4:4" line-data="public class BaseEntity implements Serializable {">`BaseEntity`</SwmToken> is marked with @<SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="23:6:6" line-data="import jakarta.persistence.MappedSuperclass;">`MappedSuperclass`</SwmToken>, which means it is not a standalone entity but provides common mapping information for subclasses. This avoids repeating the id property and its annotations in every entity class. It centralizes the identifier logic in one place, making the code cleaner and easier to maintain.

```java
import java.io.Serializable;

import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.MappedSuperclass;

/**
 * Simple JavaBean domain object with an id property. Used as a base class for objects
 * needing this property.
 *
 * @author Ken Krebs
 * @author Juergen Hoeller
 */
@MappedSuperclass
public class BaseEntity implements Serializable {
```

---

</SwmSnippet>

# how is the entity identifier managed?

<SwmSnippet path="/src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" line="35">

---

The id field is declared as an Integer and annotated with @Id and @<SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="36:2:2" line-data="	@GeneratedValue(strategy = GenerationType.IDENTITY)">`GeneratedValue`</SwmToken>(strategy = <SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="36:8:10" line-data="	@GeneratedValue(strategy = GenerationType.IDENTITY)">`GenerationType.IDENTITY`</SwmToken>). This tells JPA that this field is the primary key and its value is generated by the database upon insert. Using <SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="36:8:10" line-data="	@GeneratedValue(strategy = GenerationType.IDENTITY)">`GenerationType.IDENTITY`</SwmToken> means the database handles id generation, which fits well with typical relational databases.

```java
	@Id
	@GeneratedValue(strategy = GenerationType.IDENTITY)
	private Integer id;
```

---

</SwmSnippet>

# utility methods for entity state

<SwmSnippet path="/src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" line="39">

---

<SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="33:4:4" line-data="public class BaseEntity implements Serializable {">`BaseEntity`</SwmToken> provides a getter and setter for the id field, allowing subclasses and other code to access and modify the identifier as needed.

```java
	public Integer getId() {
		return id;
	}

	public void setId(Integer id) {
		this.id = id;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" line="47">

---

It also includes an <SwmToken path="src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java" pos="47:5:7" line-data="	public boolean isNew() {">`isNew()`</SwmToken> method that returns true if the id is null. This is a simple way to check if the entity has not yet been persisted (since a generated id will be assigned only after saving). This method helps in deciding whether to insert or update the entity.

```java
	public boolean isNew() {
		return this.id == null;
	}

}
```

---

</SwmSnippet>

&nbsp;

*This is an auto-generated document by Swimm 🌊 and has not yet been verified by a human*

<SwmMeta version="3.0.0" repo-id="Z2l0aHViJTNBJTNBc3ByaW5nLXBldGNsaW5pYyUzQSUzQXVtYWxpbmdhc3dhbWk=" repo-name="spring-petclinic"><sup>Powered by [Swimm](https://app.swimm.io/)</sup></SwmMeta>
