---
title: Database schema creation script for PostgreSQL
---
# introduction

This document explains the database schema creation script for PostgreSQL used in the project. The script defines the tables, their relationships, and indexes needed to store and query the application's core data.

We will cover:

1. How the main entities (vets, specialties, owners, pets, visits) are structured.
2. How relationships between entities are implemented.
3. The use of indexes to optimize queries.
4. How to run the script.

# main entities and their structure

The script creates tables for the main domain entities:

- **vets**: stores veterinarian details with an auto-generated integer primary key and first/last names.
- **specialties**: stores specialty names with an auto-generated primary key.
- **owners**: stores pet owner details including address and telephone.
- **types**: stores pet types (like dog, cat).
- **pets**: stores pet details including name, birth date, type, and owner.
- **visits**: stores visits for pets with date and description.

<SwmSnippet path="/src/main/resources/db/postgres/schema.sql" line="1">

---

Each table uses <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="2:3:13" line-data="  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,">`INT GENERATED BY DEFAULT AS IDENTITY`</SwmToken> for primary keys, which lets PostgreSQL auto-generate unique IDs. This avoids manual ID management and ensures consistency.

```plsql
CREATE TABLE IF NOT EXISTS vets (
  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  first_name TEXT,
  last_name  TEXT
);
CREATE INDEX ON vets (last_name);

CREATE TABLE IF NOT EXISTS specialties (
  id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT
);
CREATE INDEX ON specialties (name);
```

---

</SwmSnippet>

<SwmSnippet path="/src/main/resources/db/postgres/schema.sql" line="26">

---

&nbsp;

```plsql
CREATE TABLE IF NOT EXISTS owners (
  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  first_name TEXT,
  last_name  TEXT,
  address    TEXT,
  city       TEXT,
  telephone  TEXT
);
CREATE INDEX ON owners (last_name);

CREATE TABLE IF NOT EXISTS pets (
  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name       TEXT,
  birth_date DATE,
  type_id    INT NOT NULL REFERENCES types (id),
  owner_id   INT REFERENCES owners (id)
);
CREATE INDEX ON pets (name);
CREATE INDEX ON pets (owner_id);

CREATE TABLE IF NOT EXISTS visits (
  id          INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  pet_id      INT REFERENCES pets (id),
  visit_date  DATE,
  description TEXT
);
CREATE INDEX ON visits (pet_id);
```

---

</SwmSnippet>

# relationships between entities

Relationships are implemented using foreign keys:

- <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="14:10:10" line-data="CREATE TABLE IF NOT EXISTS vet_specialties (">`vet_specialties`</SwmToken> is a join table linking vets and specialties with a composite unique constraint to avoid duplicates.
- <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="36:10:10" line-data="CREATE TABLE IF NOT EXISTS pets (">`pets`</SwmToken> references <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="20:10:10" line-data="CREATE TABLE IF NOT EXISTS types (">`types`</SwmToken> and <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="26:10:10" line-data="CREATE TABLE IF NOT EXISTS owners (">`owners`</SwmToken> to associate each pet with its type and owner.
- <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="46:10:10" line-data="CREATE TABLE IF NOT EXISTS visits (">`visits`</SwmToken> references <SwmToken path="src/main/resources/db/postgres/schema.sql" pos="36:10:10" line-data="CREATE TABLE IF NOT EXISTS pets (">`pets`</SwmToken> to link visits to specific pets.

<SwmSnippet path="/src/main/resources/db/postgres/schema.sql" line="14">

---

Foreign key constraints enforce referential integrity, preventing orphan records and ensuring valid links between tables.

```plsql
CREATE TABLE IF NOT EXISTS vet_specialties (
  vet_id       INT NOT NULL REFERENCES vets (id),
  specialty_id INT NOT NULL REFERENCES specialties (id),
  UNIQUE (vet_id, specialty_id)
);

CREATE TABLE IF NOT EXISTS types (
  id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT
);
CREATE INDEX ON types (name);
```

---

</SwmSnippet>

# indexes for query optimization

Indexes are created on frequently queried columns to speed up lookups:

- `vets.last_name` and `owners.last_name` for searching by last name.
- `specialties.name` and `types.name` for quick specialty and type lookups.
- `pets.name` and `pets.owner_id` to find pets by name or owner.
- `visits.pet_id` to quickly find visits for a pet.

<SwmSnippet path="/src/main/resources/db/postgres/schema.sql" line="1">

---

These indexes improve performance on common queries without requiring manual index management.

```plsql
CREATE TABLE IF NOT EXISTS vets (
  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  first_name TEXT,
  last_name  TEXT
);
CREATE INDEX ON vets (last_name);

CREATE TABLE IF NOT EXISTS specialties (
  id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT
);
CREATE INDEX ON specialties (name);

CREATE TABLE IF NOT EXISTS vet_specialties (
  vet_id       INT NOT NULL REFERENCES vets (id),
  specialty_id INT NOT NULL REFERENCES specialties (id),
  UNIQUE (vet_id, specialty_id)
);

CREATE TABLE IF NOT EXISTS types (
  id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT
);
CREATE INDEX ON types (name);

CREATE TABLE IF NOT EXISTS owners (
  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  first_name TEXT,
  last_name  TEXT,
  address    TEXT,
  city       TEXT,
  telephone  TEXT
);
CREATE INDEX ON owners (last_name);

CREATE TABLE IF NOT EXISTS pets (
  id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name       TEXT,
  birth_date DATE,
  type_id    INT NOT NULL REFERENCES types (id),
  owner_id   INT REFERENCES owners (id)
);
CREATE INDEX ON pets (name);
CREATE INDEX ON pets (owner_id);

CREATE TABLE IF NOT EXISTS visits (
  id          INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  pet_id      INT REFERENCES pets (id),
  visit_date  DATE,
  description TEXT
);
CREATE INDEX ON visits (pet_id);
```

---

</SwmSnippet>

# how to run the script

The script is located at <SwmPath>[src/…/postgres/schema.sql](src/main/resources/db/postgres/schema.sql)</SwmPath>. To apply it:

1. Connect to your PostgreSQL database using a client like `psql`.

2. Run the script with a command like:

   ```
   \i path/to/src/main/resources/db/postgres/schema.sql
   ```

3. The script will create tables and indexes if they do not already exist, so it can be safely run multiple times without errors.

This sets up the database schema required for the application to store and manage its data.

&nbsp;

*This is an auto-generated document by Swimm 🌊 and has not yet been verified by a human*

<SwmMeta version="3.0.0" repo-id="Z2l0aHViJTNBJTNBc3ByaW5nLXBldGNsaW5pYyUzQSUzQXVtYWxpbmdhc3dhbWk=" repo-name="spring-petclinic"><sup>Powered by [Swimm](https://app.swimm.io/)</sup></SwmMeta>
